Documentation of how to install the AES CBC CLI.

## Dependencies

1. Create a virtual environment with python venv module

```bash
$ python -m venv venv
$ source ./venv/bin/activate
```

2. Install the dependencies using PIP package manager

```
$ pip install -r requirements.txt
```
